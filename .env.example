# =============================================================
# .env.example — copia este archivo a ".env" y cambia los valores
#
#   cp .env.example .env
#
# NUNCA subas el .env real a git (ya está en .gitignore)
# =============================================================

# ── PostgreSQL ────────────────────────────────────────────────
POSTGRES_USER=inventario_user
POSTGRES_PASSWORD=cambia_esta_password_segura
POSTGRES_DB=inventario_db
